{
  "name": "website-builder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node prerender.mjs",
    "build:dev": "tsc && vite build --mode development && node prerender.mjs",
    "preview": "vite preview",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "express": "^5.2.1",
    "firebase-tools": "^15.9.1",
    "puppeteer": "^24.39.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
